/* =========================================================================
   config.js — Conexión con Supabase
   Pon aquí los datos de TU proyecto y súbelos a GitHub. Con esto el panel
   funciona desde cualquier lugar y todos ven la misma información.

   Dónde salen estos dos valores:
     supabase.com  →  tu proyecto  →  Settings  →  API
       · Project URL   →  URL
       · anon public   →  CLAVE

   La clave "anon" está pensada para ir en la página: por sí sola no da acceso
   a nada, porque las tablas exigen haber entrado con usuario y contraseña.
   ========================================================================= */

const SUPABASE = {
  URL: '',
  CLAVE: ''
};
